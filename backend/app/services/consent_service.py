from sqlalchemy import desc, select
from sqlalchemy.ext.asyncio import AsyncSession

from app.db.models import User, UserConsent


PERSONAL_DATA_CONSENT_VERSION = "pd-consent-v4"

PERSONAL_DATA_CONSENT_TEXT = """СОГЛАСИЕ НА ОБРАБОТКУ ПЕРСОНАЛЬНЫХ ДАННЫХ

Настоящим я, субъект персональных данных (далее — «Пользователь»), свободно, своей волей и в своем интересе даю оператору сервиса «shtraf.osporit» (далее — «Оператор») согласие на обработку моих персональных данных на изложенных ниже условиях.

1. Цель обработки персональных данных

Персональные данные обрабатываются для анализа постановления и иных материалов о штрафе; проверки предоставленных сведений и документов; определения возможных направлений обжалования; подготовки проектов жалобы, ходатайств, заявлений, пояснений и иных документов; направления результатов Пользователю; взаимодействия с Пользователем; исполнения договора и требований законодательства Российской Федерации.

2. Перечень обрабатываемых персональных данных

Оператор может обрабатывать добровольно предоставленные Пользователем фамилию, имя, отчество; дату рождения, адрес, телефон и электронную почту, если они нужны для подготовки документов; сведения о транспортном средстве и документах на него; сведения из постановления; фотографии, видеозаписи и иные материалы о предполагаемом правонарушении; сведения о водителе; доказательства и иные данные, объективно необходимые для заказанной услуги.

Оператор не должен запрашивать персональные данные, которые не требуются для этих целей. Перед загрузкой рекомендуется закрыть или удалить из документа лишние персональные данные.

3. Действия с персональными данными

Согласие распространяется на сбор, запись, систематизацию, накопление, хранение, уточнение, извлечение, использование, предоставление доступа в предусмотренных законом случаях, обезличивание, блокирование и удаление персональных данных с использованием средств автоматизации или без них.

4. Передача персональных данных

Оператор вправе передавать данные третьим лицам только в необходимом объеме и при наличии законного основания. Для распознавания постановление может передаваться OCR.space. Для автоматизированного юридического анализа распознанный текст и данные дела могут передаваться DeepSeek.

Если выбранная услуга предусматривает направление документов от имени Пользователя, данные могут быть предоставлены уполномоченному суду, органу или должностному лицу в необходимом для рассмотрения объеме.

5. Автоматизированная подготовка документов

Оператор может использовать программные средства автоматизации. Результат автоматизированной подготовки не гарантирует отмену постановления, прекращение производства или решение в пользу Пользователя. До подписания и направления Пользователь обязан проверить фактические сведения в подготовленном документе.

6. Срок обработки и хранения

Данные обрабатываются в течение срока, необходимого для указанных целей и исполнения требований законодательства. После достижения целей они подлежат уничтожению или обезличиванию, если закон не требует иного.

7. Отзыв согласия

Пользователь может отозвать согласие и удалить профиль, согласия, дела и локальные файлы командой /restart. После отзыва Оператор прекращает обработку и уничтожает данные в установленных законом случаях и сроках, если отсутствует иное законное основание для обработки. Отзыв может сделать дальнейшее оказание услуги невозможным.

8. Подтверждения Пользователя

Предоставляя согласие, Пользователь подтверждает достоверность предоставленных данных; ознакомление с целями и условиями обработки; добровольность предоставления данных; понимание права на отзыв; наличие законных оснований для предоставления данных другого лица.

Настоящее согласие предоставляется в соответствии с Федеральным законом от 27.07.2006 № 152-ФЗ «О персональных данных»."""


class ConsentService:
    def __init__(self, session: AsyncSession) -> None:
        self.session = session

    async def has_current_consent(self, user_id: int) -> bool:
        consent = await self.session.scalar(
            select(UserConsent.id).where(
                UserConsent.user_id == user_id,
                UserConsent.version == PERSONAL_DATA_CONSENT_VERSION,
            )
        )
        return consent is not None

    async def latest_for_user(self, user_id: int) -> UserConsent | None:
        return await self.session.scalar(
            select(UserConsent)
            .where(UserConsent.user_id == user_id)
            .order_by(desc(UserConsent.accepted_at))
            .limit(1)
        )

    async def accept_current(self, user: User) -> UserConsent:
        existing = await self.session.scalar(
            select(UserConsent).where(
                UserConsent.user_id == user.id,
                UserConsent.version == PERSONAL_DATA_CONSENT_VERSION,
            )
        )
        if existing is not None:
            return existing

        consent = UserConsent(
            user_id=user.id,
            telegram_id=user.telegram_id,
            version=PERSONAL_DATA_CONSENT_VERSION,
        )
        self.session.add(consent)
        await self.session.flush()
        return consent
