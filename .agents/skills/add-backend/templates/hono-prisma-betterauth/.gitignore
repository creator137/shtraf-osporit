node_modules/
# SQLite databases (zero-config dev + tests) — never committed.
*.db
*.db-shm
*.db-wal
prisma/*.db
# Prisma-generated client — regenerated by `prisma generate` (postinstall).
prisma/generated/
.prisma/
.env
.env.local
dist/
